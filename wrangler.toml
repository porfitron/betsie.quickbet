# Cloudflare Pages + Functions (https://developers.cloudflare.com/pages/functions/wrangler-configuration/)
name = "betsie-quickbet"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# After you create a D1 database, uncomment and set database_id from:
#   npx wrangler d1 create betsie-quickbet
# Or paste the binding in Dashboard → Pages project → Settings → Functions → D1 database bindings
# [[d1_databases]]
# binding = "DB"
# database_name = "betsie-quickbet"
# database_id = "YOUR_DATABASE_ID"
