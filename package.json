{
  "name": "quickbet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply quickbet --local",
    "db:migrate:remote": "wrangler d1 migrations apply quickbet --remote"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
